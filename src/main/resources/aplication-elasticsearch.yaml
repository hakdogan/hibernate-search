spring.jpa:
  properties:
    hibernate.search:
      automatic-indexing:
          synchronization:
            strategy: sync
      backend:
        hosts: http://localhost:9200
        protocol: http
        discovery.enabled: true
        #analysis.configurer: elasticsearchAnalysisConfigurer