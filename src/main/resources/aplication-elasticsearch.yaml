spring.jpa:
  properties:
    hibernate.search:
      backend:
        hosts: http://localhost:9200
        protocol: http
        discovery.enabled: true
        #analysis.configurer: elasticsearchAnalysisConfigurer